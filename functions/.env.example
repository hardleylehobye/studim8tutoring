# Copy to `.env` or `.env.studim8tutoring-bba8e` in this folder (never commit real values).
# Firebase loads these when you run `firebase deploy --only functions`.

PAYFAST_MERCHANT_ID=
PAYFAST_MERCHANT_KEY=
PAYFAST_PASSPHRASE=
PAYFAST_SANDBOX=true
APP_PUBLIC_ORIGIN=https://hardleylehobye.github.io/studim8tutoring
